import { Routes } from '@angular/router';
import { ReaderShellComponent } from './features/reader-shell/reader-shell.component';

export const routes: Routes = [
  {
    path: '',
    component: ReaderShellComponent,
  },
];
